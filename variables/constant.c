// constant values are those which cannot be changed after declaring them as a consntant value and it will throw an error if we try to change it later::

// Test code
# include <stdio.h>
int main(){
    const a = 45;
    printf("The value of the const a is :: %d \n",a);

    // a = 34;
    // printf("The new value of the a is changed or will it throw an error :: %d",a)




}